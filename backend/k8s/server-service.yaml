apiVersion: v1
kind: Service
metadata:
  name: server-service
spec:
  type: NodePort
  selector:
    app: server
  ports:
    - port: 1337
      targetPort: 1337
      nodePort: 30002
      protocol: TCP
