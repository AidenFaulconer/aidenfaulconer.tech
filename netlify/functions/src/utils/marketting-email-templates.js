// ========================================================================== //
// Templates
// ========================================================================== //
// this will hold some HTML responsive email templates to complete with data
// users input for either bookings, contacts, or for other automation purposes

const bookingTemplate = (data) => {

};

const contactTemplate = (data) => {

};

module.exports = { bookingTemplate, contactTemplate };
